name: Server Docker Image Build

'on':
  push:
    tags:
      - 'build-*'

permissions:
  contents: write
  actions: read

jobs:
  server-image:
    runs-on: ubuntu-latest
    env:
      IMAGE_NAME: ai_tv_server
      TAR_NAME: ai_tv_server_latest.tar
      TAR_PATH: /tmp/ai_tv_server_latest.tar
      IMAGE_TAG: ai_tv_server:latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build server Docker image tar
        uses: docker/build-push-action@v6
        with:
          context: ./server
          file: ./server/Dockerfile
          platforms: linux/amd64
          tags: ${{ env.IMAGE_TAG }}
          outputs: type=docker,dest=${{ env.TAR_PATH }}

      - name: Upload server image artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.IMAGE_NAME }}_latest
          path: ${{ env.TAR_PATH }}
          if-no-files-found: error
          retention-days: 7

      - name: Publish tar to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ github.ref_name }}
          generate_release_notes: true
          files: ${{ env.TAR_PATH }}

      - name: Output usage
        shell: bash
        run: |
          echo "Artifact ready: ${TAR_NAME}"
          echo "Image tag inside tar: ${IMAGE_TAG}"
          echo "After download, import with:"
          echo "docker load -i ${TAR_NAME}"
          echo "Run example:"
          echo "docker run -d --name ai_tv -p 8000:8000 -e DATA_DIR=/data -e DB_PATH=/data/db/app.db -e BASIC_USER=admin -e BASIC_PASS=admin -v /srv/ai_tv_data:/data ${IMAGE_TAG}"
