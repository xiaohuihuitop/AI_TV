# 测试说明：检测 marked 正则兼容性

## 用途
检测 `marked.min.js` 是否包含 `\p{...}` 这类 Unicode 属性转义，避免在 App 运行环境中触发正则语法错误白屏。

## 使用方法
在项目根目录执行：

```bash
node AI_TOOL/test_2026_01_22_22_23_marked_unicode_regex.js
```

## 预期结果
- 输出“未发现 \p{...}，通过。”并返回 0 表示通过。
- 若输出“发现不兼容正则 ...”并返回非 0，表示失败，需要替换或降级 marked。
