# 视频描述数据库检查

## 用途
- 检查 `videos` 表是否包含 `description` 字段。

## 使用方法
- 在项目根目录执行：
```
python AI_TOOL/视频描述_数据库检查.py
```

## 预期结果
- 通过：输出 `OK: videos.description 字段已存在`。
- 失败：抛出 `videos 表缺少 description 字段` 异常。
