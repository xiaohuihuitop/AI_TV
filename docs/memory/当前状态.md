# 当前状态

## 记忆维护规则

- AI 后续执行只读取并参考 `## 当前状态`。
- `## 历史状态` 只用于追溯，不作为后续执行依据。

## 历史状态

### 上一次状态

#### 日期

- 2026-08-30

#### 当前目标

- 手机端全屏播放时常驻“上一个、返回、下一个”，且视频不被遮挡或裁切。

#### 已完成

- 页面级沉浸式播放器、独立底部导航、横竖屏切换和返回恢复已实现并在 MuMu 完整复测。

#### 下一步

- 用户确认后提交 Android 实现；实体手机复测异形屏和系统最大字体。

## 当前状态

### 日期

- 2026-09-12

### 当前目标

- 部署服务端最新改动，并完成后台手机预览播放与 APK + WGT 自动更新的线上验收。

### 已确认实施计划

| 阶段 | 状态 | 目标 | 验收点 |
| --- | --- | --- | --- |
| 客户端更新服务 | 已完成 | 启动/回前台检查并安装高版本 WGT | 版本、清单、大小、播放保护和失败清理测试通过 |
| WGT 产物 | 已完成 | 生成高于旧 APK 的可安装资源包 | `1.0.1 / 101`，306509 字节，内置更新代码 |
| 线上发布验收 | 阻塞 | 发布 `/update/` 静态目录和 `update.json` | 当前线上 `update.json` 返回 404 |

### 进行中

- 等待线上反代配置 `/update/`，并在旧版 `1.0.0` APK 上完成真实安装重启验证。

### 已完成

- 自动更新服务和生命周期接入已完成；全部 Android Node 回归、Python `compileall` 和 `git diff --check` 通过。
- 已生成 `android/unpackage/release/wgt/ai-tv-1.0.1.wgt`，并确认 WGT manifest 版本为 `1.0.1 / 101`、不含 `adid`。
- 服务端 public 清单已修复 `X-Forwarded-Proto` 处理，新增回归确认 HTTPS 反代下视频和封面 URL 使用 HTTPS。
- 本地生成的 APK 资源 manifest 含 `adid: 122993130201`，已确认这是 DCloud/HBuilderX 云端配置注入，不是源码配置。
- 服务端手机预览已改为直接展示 `20:9` 横屏和 `9:20` 竖屏手机模型；视频完整显示，并提供播放、进度、时长和底部三按钮模拟。本地可播放横竖屏素材已完成视觉与交互验收。
- 已修复 API/public 视频 Range 响应漏传 `video/mp4` 的问题，并新增两个接口的 206 响应回归。

### 下一步

- 在域名反代增加 `/update/` 静态目录，先上传 WGT，再发布 `update.json`。
- 重新构建/加载服务端镜像，确认公网 public 清单资源 URL 已从 HTTP 变为 HTTPS，后台预览视频的 206 响应具有 `Content-Type: video/mp4`。
- 用旧版 APK 验证自动下载、安装、重启和播放页保护；清单缺失或 WGT 失败时确认旧版本仍可用。
- 在 DCloud/HBuilderX 清空广告 AppID 后重新生成 APK，再做无广告检查。

### 阻塞与待确认

- `https://tv.xiaohuihuitop.top/update/update.json` 当前返回 `404`，线上静态目录还没有配置。
- 公网当前仍运行旧服务端镜像，public 清单仍生成 HTTP 资源地址；源码修复尚未部署。
- 当前 APK 含 `adid: 122993130201`，需要用户在 DCloud/HBuilderX 云打包设置中关闭广告配置后重新打包。
- 当前没有 Android 模拟器设备连接，WGT 的真实安装重启尚未验证。
- 线上尚未部署本次 Range 媒体类型修复；若部署后预览仍失败，需要在服务器使用 `ffprobe` 检查该视频编码。

### 关联决策

- `docs/superpowers/specs/2026-08-30-server-mobile-preview-design.md`
- `docs/superpowers/plans/2026-08-30-server-mobile-preview.md`
- `docs/superpowers/plans/2026-08-30-server-reliability-hardening.md`
- `docs/superpowers/specs/2026-09-01-wgt-auto-update-design.md`
- `docs/superpowers/plans/2026-09-01-wgt-auto-update.md`
